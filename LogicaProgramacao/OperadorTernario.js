/**
 *   ?
 *   :
 * Forma mais pratica do que IF ELSE
 *  ? 'Valor para verdadeira' : 'Valor para Falso'
 */

const pontucao = 1000;
const nivelUsuario = pontucao >= 1000 ? "Usuario Vip" : "Usuario normal";
console.log(nivelUsuario);
